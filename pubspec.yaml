name: p2p_chat
description: Chat P2P seguro com Tox

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  qr_flutter: ^4.2.0
  mobile_scanner: ^5.2.3     # Versão moderna compatível com Kotlin recente
  google_fonts: ^6.2.1
  flutter_animate: ^4.5.0
  shared_preferences: ^2.3.2  # Atualizado
  sqflite: ^2.4.0              # Atualizado
  cryptography: ^2.7.0
  path_provider: ^2.1.4
  ffi: ^2.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
